{
  "name": "cashflow-ledger",
  "version": "0.1.0",
  "private": true,
  "description": "Outstanding-payment tracker for a wholesale FMCG distributor. Offline-first, no npm dependencies.",
  "type": "module",
  "engines": { "node": ">=22.5" },
  "scripts": {
    "start": "node server/src/index.js",
    "seed": "node server/src/seed.js",
    "test": "node --test server/test/*.test.js"
  }
}
